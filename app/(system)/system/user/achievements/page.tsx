import SystemHeader from "@/components/ui/SystemHeader";
import React from "react";

type Props = {};

const Achievements = (props: Props) => {
  return <SystemHeader title="Conquistas" />;
};

export default Achievements;
